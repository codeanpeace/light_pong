class Game() {
  Player p1, p2, p3, p4;
  
  void setUp() {
    // intro sequence
  }

  void begin() {
    // kickoff   
  }
  
  void pause(Player player) {
    // pause
  }
  
  void continue(Player player) {
    // unpause only if by player that paused
  }
  
  void winning(Player player) {
    // winning sequence
  }
  
  void reset() {
    // new game logic - back to setup
  }
}
